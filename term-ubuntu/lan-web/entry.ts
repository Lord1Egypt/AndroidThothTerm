// The two things the LAN terminal page uses, bundled from xterm.js's
// TypeScript sources (not its prebuilt, minified lib/).
export { Terminal } from 'browser/public/Terminal';
export { FitAddon } from '@xterm/addon-fit/src/FitAddon';
